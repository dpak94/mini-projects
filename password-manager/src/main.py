# Password Manager

prompt_masterPWD = input("What is the master password?")
mode = input("Would you like to add a new password or access existing ones?").lower()

if mode == "q""
    break

if mode == "view":
    pass

